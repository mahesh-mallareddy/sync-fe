'use client'
import Shell from "@/components/app/shell"

function adminlayout({ children }) {
    return (
        <Shell>
            {children}
        </Shell>
    )
}

export default adminlayout
    